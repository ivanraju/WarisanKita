name: warisan_kita
description: "A premium educational directory for traditional Malaysian arts."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.8

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  provider: ^6.1.1
  supabase_flutter: ^2.6.0
  flutter_staggered_grid_view: ^0.7.0
  google_fonts: ^6.1.0
  url_launcher: ^6.2.5
  http: ^1.2.0
  file_picker: ^12.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
